<h3>Log In!</h3>
<br/>
<%= form_for :user, url: '/users' do |f| %>

  <%= f.label :email %>:
  <%= f.text_field :email %>

  <!-- <input type="text" name='[attempt_login][email]'> -->

  <%= f.label :password %>:
  <%= f.password_field :password %>

  <%= f.submit "Submit" %>
<% end %>
